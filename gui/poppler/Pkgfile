# Description: PDF rendering library based on xpdf 3.0
# URL: URL: http://poppler.freedesktop.org/
# Packager: alienus at nutyx dot org
# Depends on: xorg-libxxf86vm libpng xorg-fontconfig libjpeg-turbo cairo libxml2 openjpeg lcms2 gtk2 qt4 qt5 gobject-introspection 

dataversion=0.4.7
name=poppler
version=0.43.0
release=1

source=(
http://poppler.freedesktop.org/$name-$version.tar.xz
http://poppler.freedesktop.org/poppler-data-$dataversion.tar.gz)

build() {
cd poppler-$version
 
./configure --prefix=/usr                     \
            --sysconfdir=/etc                 \
            --disable-static                  \
            --enable-build-type=release       \
            --enable-cmyk                     \
            --enable-xpdf-headers             \
            --with-testdatadir=$PWD/testfiles
make
make DESTDIR=$PKG install

cd ../poppler-data-$dataversion
make prefix=/usr DESTDIR=$PKG install
}
