# Depends on: gtk2 libglade gconf gtk-vnc libgnome-keyring dconf libpeas telepathy-glib vte3 libsecret gtk-vnc

description="A VNC Client for the GNOME Desktop."
maintainer="http://linuxfromscratch.org/"
packager="tnut at nutyx dot org"

name=vinagre
version=3.22.0
release=1
source=($name-$version-freerdp-01.patch
	http://download.gnome.org/sources/$name/${version%.*}/$name-$version.tar.xz)

build() {
	cd $name-$version
	patch -Np1 -i ../$name-$version-freerdp-01.patch
	./configure --prefix=/usr \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib/$name \
	--enable-rdp \
	--enable-spice
	make
	make DESTDIR=$PKG install
}
