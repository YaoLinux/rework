# Description: efl libraries
# URL: http://www.enlightenment.org/
packager="tnut at nutyx dot org"
# Depends on: luajit fribidi giflib poppler libspectre gstreamer1 libraw librsvg pulseaudio xorg-libxscrnsaver bullet gstreamer1-plugins-base

name=efl
version=1.18.0
release=1
source=(http://download.enlightenment.org/rel/libs/efl/$name-$version.tar.gz)
#
build() {
    cd $name-$version
    ./configure --prefix=/usr \
                --disable-gstreamer \
                --enable-gstreamer1 \
                --enable-multisense \
                --with-tests=none \
                --with-opengl=full \
                --disable-doc
    make
    make DESTDIR=$PKG install
}
